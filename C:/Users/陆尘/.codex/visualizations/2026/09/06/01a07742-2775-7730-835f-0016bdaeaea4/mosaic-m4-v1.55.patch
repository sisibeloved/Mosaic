--- a/docs/plan/2026-08-25-delivery-plan.md
+++ b/docs/plan/2026-08-25-delivery-plan.md
@@ -5,8 +5,8 @@
 | 项目 | 内容 |
 |---|---|
 | 文档类型 | 交付与进度规划（进度归进度：本文不改设计结论，只裁定交付范围与顺序；设计变更走 RFC/ADR） |
+| 版本 | v1.55 |
+| 日期 | 2026-09-06 |
-| 版本 | v1.54 |
-| 日期 | 2026-09-01 |
 | 拟制 | Mosaic 项目组 / ZCode |
 | 上游 | [架构设计说明书](../design/2026-08-13-mosaic-architecture-design.md) v0.9；[RFC-0001～0011](../design/rfc/)；[ADR-0001～0007](../design/adr/)；[Harness 调研报告](../design/research/2026-08-25-harness-survey.md) |
 | 交付目标 | **个人完全可用的 App**：单用户本地运行，一等公民支持 Windows 与 macOS；不是验证性半成品 |
@@ -71,6 +71,7 @@
 | v1.52 | 2026-09-04 | 陆尘裁定 / ZCode | **权限档裁定范围纠正（"codex 也有权限策略啊……各个 Agent 要么有 YOLO 模式，要么权限可以设置成 Full 级别的，只是名称不一样，都得适配上"）——v1.51 把 codex 排除出裁定域、把 kimi --auto 当权限档均有误**。逐家落地与实证：(1) **codex**：sandbox 即其权限面（exec 非交互下审批本就 never，-s 是唯一闸）——`-s read-only` → **`-s danger-full-access`**（Full 档；read-only 曾连网络一并封死；实证 0.151 full 档最小调用跑通；resume 子命令不接受 -s——沙箱随会话首轮固定，首轮 Full 即全会话 Full；argv 契约 UT 钉死 TestRuntimeSandboxFullCodex）；(2) **kimi**：YOLO 档是 `-y/--yolo`（"Auto-approve regular tool calls"，v1.51 误把 --auto 当权限档——auto 是"完全自治模式"且与 -p 互斥），实证 **--yolo 同样与 -p 互斥**（"Cannot combine --prompt with --yolo."）——-p 是 kimi 唯一无头通道（2026-08-31 实证无 --input/文件通道），该形态自带审批语义（工具直接执行，狗粮 web 抓取已证可用），无旗标可叠加，维持现状并注记；(3) mcode 已于 v1.51 落地 `--permission full`。三家对齐结果：**能在无头形态显式设 Full 的都设了（codex/mcode），形态本身自带放行的维持并注记（kimi）**；爆炸半径统一由 per-profile 工作目录与机主信任域承担。登记：codex 0.151 exec 无 --search 旗标（web 工具面未开放，与权限无关） |
 | v1.53 | 2026-09-04 | Mosaic 项目组 / ZCode | **dogfood 诊断+修复（"MiniMax 说开抓 10 分钟无动静"）——执行模型认知错配**。三层取证：(1) 事件流/日志时间线（DB=UTC/日志=本地 MDT 对齐）——07:12Z minimax 发"开抓，10 分钟内回前两条"（带 mosaic-todo 申报块，v1.49 协议已被模型自发使用 ✓）+ 07:12Z/07:17Z 两波全静默（minimax rationale："再说话是空转，留在执行上"）；(2) **mcode 会话日志实锤：宣言轮（07-12-08 会话）content 只有 text/thinking、零工具调用**——模型只是"说"了要抓；(3) 引擎侧无恙——主动波 5min 起搏正常（07:17:25Z proactive=true 实证，07:22:57Z 会再来）、Timeout 装配 180s 非瓶颈。**根因：mcode exec 按轮拉起、轮末即结束、无后台执行，而模型抱持"我会在后台执行 10 分钟"的自我模型**——两轮机会均以"执行中"为由自选静默，tasklist_note 的"没有进展就说明情况"被解读成"无话可说"而非"任务停滞"。修复（认知矫正进协议指令，两处单源）：TasklistProtocol 增执行模型硬事实（"你的会话按轮拉起、轮末即结束，没有后台执行——任务须在某一轮回复中当场完成，只承诺不执行任务永远停在原地"）；主动波 tasklist_note 改写（"'已在执行中'不是静默理由，要么本轮就做（调用工具当场完成），要么说明真实阻塞"）。登记：若矫正后仍空转，下一步候选为评估指令面向主动波的条件化（intentInstruction 静态通用文案与 tasklist_note 的张力）+ 生成任务超时按抓取类任务放宽（180s 对多页抓取+撰写余量未实证） |
 | v1.54 | 2026-09-04 | Mosaic 项目组 / ZCode | **M3 完成度核对与收口（负责人指令"确认 M3 规划内容是否已完成，没有完成的补上"）**。逐切片对照代码核实：M3-2/3-6 已按既有裁定落地（RFC-0005 附录 B 群聊制裁剪 / v1.36 数据主权），本版勾选并补注记；**两个真实缺口补齐**：(1) **M3-5 认领**——claim_evidence_request 命令 + evidence_request.claimed 事件（owners 追加、open 态单次、同人不重复、终态后拒绝；幂等投影去重；命令面仅 human/system，agent 自主认领随工具面分期）+ Schema/fixtures/gate 反例 + [dev] 文案 + 命令枚举 21 + 认领命令链 UT；(2) **M3-4 Claim 投影（"按 flag 离线启用"）**——ClaimsOf 确定性最小账本（零 LLM）：胶囊结论→strengthened、同 thread 后到胶囊取代早者→superseded（重开后再收束的演化留痕）、假设→open、证据需求单问题→open_question（resolved→strengthened/dismissed→weakened）；取编辑后胶囊视图（与注入同源）；"按 flag 离线启用"落地为仅 dev 门禁端点 GET /v1/debug/rooms/{id}/claims（不入组装/公开快照）+ 端点 UT（含非 dev 404）；RFC-0006 E-03 模型混合提取为其后续分期。**贯穿观测读数归档（出口判据项）**：GPT-6 预测讨论房 195 带 timing 收波——波全程 p50 17.8s/max 201.9s，评估Σ p50 16.9s 占 ~95%（完全主导）；逐座评估 p50 codex 14.1s/kimi 9.6s/minimax 14.6s，kimi 方差极端（max 109.3s）；Σ→max 并行化节省 max 164.5s——**裁定建议：评估相并行化（不改发布语义）**；M3-1 狗粮基座与 M3-2/3-4/3-5/3-6 全部勾选；出口判据四项三项 ✓（bounded_disagreement 真实讨论实录待狗粮——机制 UT/IT 钉死） |
+| v1.55 | 2026-09-06 | 陆尘裁定 / Codex | Hermes Bot Mode 源码借鉴纳入 M4：拆为执行生命周期、协作反馈、限定场景单次回复、稳定 Bot 身份与路由、监控变化检测五个切片，补充依赖、验收、来源与扩围后的工期重估。负责人明确：单次 reply-or-pass **仅适用于明确点名单人、已有任务交付**；其他场景保留意图评估与生成两阶段流程，不允许因实验收益自动扩面。常规群聊、token 统计、人工暂停与既有产品化交付沿用现行裁定。 |
 
 # 1. 交付目标与"完全可用"定义
 
@@ -236,9 +237,15 @@
 
 - **里程碑出口判据**：一场真实讨论以 bounded_disagreement 收束并成功按新证据重开（**机制 UT/IT 钉死，真实讨论实录待狗粮——v1.54 核对注记**）；导出包在干净环境重放一致（✓ v1.36 UT）；删除后全库无残留（✓ fixture UT）；三 agent 狗粮观测记录归档（✓ v1.54：195 波 timing 读数与裁定建议归档于贯穿观测项）。
 
+## M4 产品化——"不是半成品"专项（原估 4 周，v1.55 增补后待重估）
+
+目标：把工程闭环变成任何人可安装可日常使用的产品。验收对照第 1 节 DoD，并纳入以下五个 Hermes Bot Mode 借鉴切片。
+
+**范围来源（2026-09-06，负责人确认）**：基于 Hermes Agent v0.21.0（标签 `v2026.8.31`）源码对照，将执行生命周期、协作反馈、限定场景单次回复、稳定 Bot 身份与路由、监控变化检测纳入 M4。**M4-3 单次回复只适用于“明确点名单人”和“已有任务交付”两种场景；其他场景不适用，继续使用现有意图评估与生成流程。** 这是适用范围约束，不能因实验收益而自动扩展。
+
+落地点沿用 Mosaic 的 Go Room Runtime、SQLite 权威 Event Log、outbox 和异构 Harness Adapter。常规群聊沿用 RFC-0012 的消息驱动、自主静默与人工暂停；评估相已并行化，本轮新增项不重复安排评估并行化。Hermes 的固定轮数/消息数上限不作为新增需求，token 成本沿用现行统计口径。Hermes 标签内的 hosted-room 状态机仅作为设计与测试参考，不将其存在当作默认桌面 Bot Mode 已接入该路径的证明。
-## M4 产品化——"不是半成品"专项（4 周）
 
+### M4-0 既有产品化交付
-目标：把工程闭环变成任何人可安装可日常使用的产品。验收对照第 1 节 DoD 全表。
 
 - [ ] Wails 壳：安装器（Windows NSIS/绿色版 + macOS dmg）、应用图标、托盘常驻、首次启动向导（存储初始化 → CLI 检测/安装引导 → agent 登录态检查 → 建房演示）
 - [ ] macOS ad-hoc 签名 + 首次打开指引（个人分发不做公证：未购 Apple 开发者账号，指引右键→打开或 `xattr -cr`；若未来公开分发再评估购号公证）；Windows 代码签名（可选，无则 SmartScreen 提示文案）
@@ -247,7 +254,85 @@
 - [ ] 72h 无人值守稳定性专项：内存/句柄监控、泄漏修复、崩溃自恢复演练（双平台各一轮）
 - [ ] i18n 中英；快速上手 + FAQ + 本地隐私说明
 - [ ] 反半成品走查：全 UI 走查失败态文案与恢复建议；无占位功能
+
+### M4-1 执行生命周期与长任务回传
+
+**目标**：用户能区分“有人承诺做事”“任务已被执行器接受”“正在执行”“等待条件”“结果已经回传”。承接 v1.53“宣称后台执行而实际上轮末结束”的实证问题，tasklist 继续表示带责任人的承诺，执行记录单独表示真实运行。
+
+- [ ] 补充 RFC-0002 的执行契约与适配器能力声明：任务身份与执行尝试分离，明确任务/房间/线程/责任人关联、执行与取消代次、运行句柄、结果与回传状态；区分排队、执行中、等待用户/外部条件、完成、失败、取消中/已取消、执行结果未知。状态由实际执行器回执或运行事件产生，不能由正文中的“正在执行”推断。
+- [ ] 实现独立于单次群聊回复的长任务执行与结果回传：只有真实支持该能力的适配器可声明后台运行；不支持者明确展示当前能力。聊天与执行共享可追溯关联，既有 tasklist 的 requester/owner、人工裁定与结案规则继续生效。
+- [ ] 持久化任务接受、执行尝试、终态与投递进度；进程重启后先核对运行句柄/终态回执。无法确认是否执行过时进入“结果未知”，先查询或显式处理，不自动重新执行可能已产生副作用的任务。
+- [ ] 区分活跃长任务、等待输入和执行超时；正在运行的任务不得被新一轮重复提交。迟到结果按原始任务和线程归属回收，幂等投递；取消或代次过期的结果保留审计记录，不能作为当前有效结果发布。
+
+**验收**：UT/IT 覆盖重复接受与回传、取消后迟到成功、旧执行代次提交、来源顺序恢复、未知执行状态处理；ST 覆盖长于当前单轮时限的真实/可控执行、断线重连、服务重启与结果回到原线程。用户能从任务条目判断是否确实启动，不能再出现“承诺文本显示执行中、实际没有运行”的假状态。
+
+### M4-2 协作状态与用户反馈
+
+**目标**：承接现有 TypingBar、tasklist 与 M2 的子 Agent 状态 backlog，让用户直接理解协作进展和中断原因。
+
+- [ ] 先核对 Codex/Kimi/MiniMax 流式事件与运行查询能力，形成状态映射；复用已有排队/评估/生成/校验/草稿显示，补齐主动沉默、运行失败、执行超时、等待用户、等待外部条件、暂停/取消与迟到结果送达。
+- [ ] 在房间和任务 Tab 展示责任人、提出方、关联消息/线程、当前执行状态与可用操作；点击任务可定位承诺、执行记录与结果。已有真实澄清/输入请求在适配器支持时映射为房间内可处理卡片，未暴露的状态显示未知。
+- [ ] 增加可折叠协作活动记录，状态来自权威事件/真实运行回执，瞬态进度使用既有流通道；普通用户无需打开开发者日志即可区分“不想说”“还在做”“等我处理”“失败了”。
+- [ ] 保持静默与正式收束的语义区分：全员暂时无回复不是达成共识；失败、不可用或超时也不能被显示成同意。关键终态可重建，瞬态显示不成为另一份权威任务账本。
+
+**依赖与验收**：状态映射和静默/错误文案可先行；长任务状态接 M4-1。组件与浏览器流程覆盖上述状态、断线后的状态重建、原线程跳转、重复回调去重和键盘可访问性；每种阻塞/失败均有准确文案与符合适配器能力的恢复操作。
+
+### M4-3 单次 reply-or-pass 限定路径
+
+**负责人范围约束**：只适用于下表两类场景，资格必须在调度前可明确判断；未命中或有歧义即走现有两阶段流程。
+
+| 允许场景 | 进入条件 |
+|---|---|
+| 明确点名单人 | 消息明确请求唯一一个可解析的 Agent 响应；普通正文/引用中的名字不算点名，多人点名与身份歧义不进入 |
+| 已有任务交付 | 已定位既有任务及唯一责任人，当前行为确属该任务的结果交付；新建承诺、泛泛进展询问或未定位任务不进入 |
+
+- [ ] 实现显式资格检查与拒绝原因；该路径只作用于对应 Agent，其他成员仍按现有流程自主判断。普通群聊、开放讨论、多方交锋、多人点名、未定向主动开口、收束表态均保留意图评估与生成两阶段流程。
+- [ ] 为满足条件且声明支持的适配器提供一次调用的结构化 reply-or-pass 输出；保留责任人、任务/消息引用、公开回应或沉默决定与来源信息。记录实际走过的执行路径，不伪造一份独立评估产生的评分。
+- [ ] 保留生成前上下文新鲜度、房间发布顺序、取消/代次围栏、严格输出校验和幂等回传。资格不符或能力不支持时回退现有流程；调用已开始后的超时/结构错误按执行状态处理，不能用“回退”盲目重跑可能已执行的工具任务。
+- [ ] 仅在上述两类场景开展与现有流程的 A/B 测量，覆盖三家真实适配器的可支持组合，记录首条有效回复时间 p50/p95、调用次数、重复/遗漏、回复质量与可追溯性；按场景分别决定采用或保留原路径。结论不得推广为全场景合并评估与生成。
+
+**验收**：资格正反例 fixture 必须覆盖唯一点名、多人/歧义/引用点名、已有任务交付、新承诺、未定位任务与普通多方讨论；证明所有不适用场景仍走两阶段流程。两类场景各有测量记录，只有延迟收益明确且质量、事件语义、取消与幂等无退化者启用；不满足者保留原流程并登记结果。
+
+### M4-4 稳定 Bot 身份、固定会话入口与显式路由
+
+**目标**：同一 Agent 的身份可持续识别，重命名、切换页面、重连或更换运行实例不会把消息送给另一个 Agent。
+
+- [ ] 分离稳定 Bot 身份、展示名称、Harness profile、Executable 实例与运行连接；映射到已有 Profile/Participant/ExecutableRef，登记迁移与恢复规则。Mosaic 使用自身稳定 ID 和显式关系，不以可编辑会话标题承担唯一身份。
+- [ ] 增加 Bot 联系人/固定私聊入口的产品切片：同一身份始终回到对应长期会话；群聊参与会话按房间与成员独立映射。明确跨房间/私聊记忆的使用范围，避免仅因入口复用而混入其他会话内容。
+- [ ] 调用携带明确的目标连接、profile 与可执行实例，覆盖本地/WSL，并为已登记远端连接提供同一寻址契约与最小双连接验证；来源连接丢失时展示不可达，不能回落到当前窗口选中的连接。跨机器传输与注册细节在 RFC-0002 扩展中明确后落地。
+- [ ] 区分会话确实不存在与暂时查不到；重连不误建会话，运行实例替换不复用旧会话，删除/同名重建不复活旧映射。
+
+**验收**：同名 Bot 分属不同实例/连接、原生与 WSL 并存、改名、切换当前页面、断线重连、旧查询晚返回、连接移除、同名房间重建均有回归测试；长期会话入口稳定，群聊结果归属正确。远端扩展先用受控双连接完成契约验证，产品能力展示与实际可用性一致。
+
+### M4-5 监控变化检测与有效唤醒
+
+**目标**：监控任务先做轻量变化检测，将有意义的新输入送给 Agent；普通讨论的自主参与不受监控去重规则限制。
+
+- [ ] 对显式监控源（脚本/URL/可查询状态）建立稳定快照与摘要；无变化时记录 no-change 并跳过该监控源对应的模型调用，有变化时提供差异与最新快照。
+- [ ] 将数据源失败、无变化、模型处理失败与结果投递失败分别记录。观察水位、处理水位和送达水位分开，避免“先记哈希、模型失败后永远漏报”；去重通知与执行重试分别处理。
+- [ ] 唤醒条件分别识别新消息、未交付任务、到期事项与外部变化。房间无新消息不能成为总开关；不抑制已有任务交付、时间触发或有效外部事件，也不把监控哈希套用到普通主动开口。
+- [ ] 接入 M4-1 的执行记录与 M4-4 的固定目标，将结果投到正确 Bot 会话/房间/线程；执行中收到新变化时记录待处理水位，避免丢变化或重复启动同一任务。
+
+**验收**：相同源连续检查无模型调用；变化、首次观察、源失败后恢复、处理失败重试、投递失败补投均有可控 fixture；覆盖无新房间消息但有未交付任务/到期事项/外部变化的唤醒正例，以及普通多方讨论不受该规则影响的反例。
+
+### M4 依赖、交付顺序与出口
+
+推荐顺序：M4-2 现有状态反馈先行 → M4-1 真实执行生命周期 → M4-2 接入完整任务状态；M4-3 的资格与测量切片可独立推进，已有任务交付的最终接线依赖 M4-1；M4-4 的稳定身份优先服务执行路由，联系人及跨连接能力随后；M4-5 接 M4-1/M4-4。既有安装、升级、备份与双平台稳定性持续推进。
+
+- [ ] 实现前补充对应 RFC/ADR：RFC-0002 承载执行生命周期、能力与路由；RFC-0012 承载两类单次回复资格、实际路径记录与唤醒语义；存储迁移和会话身份变化登记 ADR。本文是交付规划，不将候选字段或状态名视为已经发布的协议。
+- [ ] 将上述正反例纳入 §5 的 UT/IT/ST 分层及真实界面验收；优先移植契约和故障场景，具体代码与现有 Mosaic 事件、适配器、存储接口对接。
+- [ ] 重估 M4 排期：原 4 周估算仅覆盖既有产品化范围；五项增补及跨连接能力不默认被原工期吸收。开工细化时按依赖和适配器能力给出新估算，既有产品化验收继续作为必交付项。
+- **出口判据**：第 1 节 DoD 按现行已接受 RFC 修订口径全绿（群聊、收束与预算采用最新裁定）；M4-1/2/4/5 的交付与验收完成，M4-3 在两类允许场景分别形成采用/保留原流程的有证据结论，全部不适用场景保持两阶段流程；双平台各一台干净机器完成全新安装→向导→首场讨论，并完成任务状态、长任务回传与恢复流程的录屏存档。
+
+### M4 参考源码（固定 Hermes v0.21.0 标签，仅作借鉴）
+
+| 切片 | 参考入口 |
+|---|---|
+| M4-1 | [成员运行/迟到回收](https://github.com/NousResearch/hermes-agent/blob/v2026.8.31/apps/desktop/src/plugins/hermes-bots/group-turns.ts)、[持久执行状态机](https://github.com/NousResearch/hermes-agent/blob/v2026.8.31/gateway/hosted_room_driver.py)、[重启/取消/幂等回归](https://github.com/NousResearch/hermes-agent/blob/v2026.8.31/tests/gateway/test_hosted_room_driver.py) |
+| M4-2 | [活动状态](https://github.com/NousResearch/hermes-agent/blob/v2026.8.31/apps/desktop/src/plugins/hermes-bots/group-activity.ts)、[等待输入与迟到结果回归](https://github.com/NousResearch/hermes-agent/blob/v2026.8.31/apps/desktop/src/plugins/hermes-bots/group-turns.test.ts) |
+| M4-3 | [reply-or-pass 提示词与群聊循环](https://github.com/NousResearch/hermes-agent/blob/v2026.8.31/apps/desktop/src/plugins/hermes-bots/group-rounds.ts)；仅借单次调用形态，适用范围服从本节负责人限定 |
+| M4-4 | [固定会话入口](https://github.com/NousResearch/hermes-agent/blob/v2026.8.31/apps/desktop/src/plugins/hermes-bots/canonical-chat.ts)、[显式路由](https://github.com/NousResearch/hermes-agent/blob/v2026.8.31/apps/desktop/src/plugins/hermes-bots/routing.ts)、[身份/连接竞态回归](https://github.com/NousResearch/hermes-agent/blob/v2026.8.31/apps/desktop/src/plugins/hermes-bots/routing.test.ts) |
+| M4-5 | [监控变化检测](https://github.com/NousResearch/hermes-agent/blob/v2026.8.31/cron/monitor.py)；处理/送达水位按 Mosaic 的失败恢复契约分别设计 |
-- **出口判据**：第 1 节 DoD 1–10 全绿（由用户按清单逐项验收）；双平台各一台干净机器完成全新安装→向导→首场讨论全程录屏存档。
 
 ## M5 第二梯队与打磨（M4 后并行/后续，2–3 周）
 
@@ -264,10 +349,10 @@
 | M1 核心闭环 | 4 周 | 6 |
 | M2 可用讨论体验 | 4 周 | 10 |
 | M3 收束/记忆/投影 | 3 周 | 13 |
+| M4 产品化 | 原估 4 周；v1.55 增补后待重估 | 原基线 17；待重估 |
+| M5 二梯队与打磨 | 2–3 周（可并行尾部） | 原基线 ~18–20；随 M4 重估 |
-| M4 产品化 | 4 周 | 17 |
-| M5 二梯队与打磨 | 2–3 周（可并行尾部） | ~18–20 |
 
+原范围基线约 4.5–5 个月交付 v1.0.0；v1.55 的 M4 五项增补需重估后更新累计工期，不沿用原日期作为扩围后的承诺。v1.0.0 的定义不含 ZCode 适配器（分级晋级语义，RFC-0002 v0.5）。
-约 4.5–5 个月交付 v1.0.0；v1.0.0 的定义不含 ZCode 适配器（分级晋级语义，RFC-0002 v0.5）。
 
 # 5. 双平台工程要求（贯穿所有里程碑）
 

