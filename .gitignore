# Local configuration and secrets
.env
.env.*
!.env.example

# JavaScript / TypeScript
node_modules/
.next/
dist/
coverage/
*.tsbuildinfo

# Go
bin/
*.test
*.out
*.exe

# Python（docs/design/icon/.venv 等）
.venv/
__pycache__/

# Runtime data
data/
tmp/
*.db
*.sqlite
*.log

# Editors and operating systems
.idea/
.vscode/
.DS_Store
Thumbs.db

# apps/web/dist/ 前端构建产物不入库（计划 v1.22，撤销 v1.7 入库裁定）：
# 由 npm run build 生成——tools/scripts/build.sh / wails build（frontend 钩子）/ CI 首步。
# go:embed 编译期解析 dist，缺失时 go build/vet/test 直接红（有意，不静默降级）。
