/* eslint-disable */
/**
 * This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */

/**
 * 文档修订批（RFC-0014 §2.3）：ops 为一批块操作（防抖聚合，事件频率与聊天消息同量级）；base_version 为文档级乐观并发期望，不符即拒绝并在新态上重放本地 ops（不引入 OT/CRDT）；version 单调递增兼任 SSE cursor。
 */
export interface DocRevisionCommittedPayload {
  doc_id: string;
  /**
   * 修订基于的版本（CAS 期望；首修订为 0）
   */
  base_version: number;
  /**
   * 本批落库后的文档版本（= base_version + 1）
   */
  version: number;
  /**
   * 块操作批（护栏 ≤64 ops / 128 KiB，RFC-0014 §2.9）
   *
   * @minItems 1
   * @maxItems 64
   */
  ops: [DocOp, ...DocOp[]];
  actor: string;
  /**
   * 落笔来源（agent 经 doc_ops 引擎代写，actor 记为该 bot）
   */
  source: "human_editor" | "agent";
  note?: string;
}
/**
 * 单条块操作：insert_after（block_id 锚点，null=插入文首，携带 block）/ append（追加文末，携带 block，block_id 恒 null）/ replace（block_id + text 替换正文）/ delete（block_id）。
 */
export interface DocOp {
  op: "insert_after" | "replace" | "delete" | "append";
  block_id: string | null;
  block?: DocBlock;
  text?: string;
}
/**
 * 块模型（RFC-0014 §2.2：稳定 block_id 是锚定操作、冲突重放与卡片摘要的共同基础；text 为 markdown 行内语法）。
 */
export interface DocBlock {
  block_id: string;
  type: "heading" | "paragraph" | "list" | "code" | "quote" | "hr";
  text: string;
}
